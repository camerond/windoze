---
layout: test
---

.fixtures
  .link_trigger
    %a(href='#') This will trigger the modal specified in the function call (defaults to #modal)
  .delegated_trigger
    %a.one(href='#') This is to test trigger delegation
    %a.two.trigger(href='#') This is to test trigger delegation
  .remote_load
    %a(href='/test/modal.html') This will load a remote file.
  .remote_image
    %a(href='/images/test.jpg') This will load a remote image.